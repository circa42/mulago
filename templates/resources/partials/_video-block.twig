

<div class="xs:w-full lg:w-1/2">
  <a href="{{ entry.url }}" class="hover:border-gray-800">
    <div class="bg-white p-4 min-h-xl m-4">
        <div class="flex justify-center mb-4 text-center">


            {% set blocks = entry.videoMatrix.limit(1).all() %}
              {% for block in blocks %}

                {% if block.type == 'videoBlock' %}
  
                  {% if block.videoId|length %}
                  <div class="overflow-hidden">
                    <iframe width="540" height="310" src="https://www.youtube.com/embed/{{ block.videoId }}" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                  </div>
                  {% endif %}
                {% endif %}
              {% endfor %}  
 

        </div>
         <h3 class="uppercase text-gray-800 hover:text-red">
          {{ entry.title }}
        </h3>
        <small class="">{{ entry.postDate | date("M d, Y") }}</small>
        <article class="text-gray-600 pt-4">
          {{ entry.excerpt |raw }}
        </article>
        <div class="text-right pt-4 text-sm">
            <a href="{{ entry.url }}" class="right uppercase text-blue-800 hover:text-blue-600">
              Read more » 
            </a>
        </div>
      
    </div>
  </a>
</div>
{# 



{% if entry.type == 'videos' %}
      
      {% set blocks = entry.videoMatrix.all() %}
      {% for block in blocks %}
        {% if block.type == 'videoBlock' %}
          
          {% if block.videoId|length %}
          <div class=my-12>
            <iframe width="660" height="315" src="https://www.youtube.com/embed/{{ block.videoId }}" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
          </div>
          {% endif %}
          {% if block.videoCopy |length %}
          <div class="text-xl leading-loose text-gray-800 mt-8">
            {{ block.videoCopy |replace({
                '<ul>': '<ul class="list-disc py-4 pl-8">',
                '<p>': '<p class="py-2">',  
                  })|raw}}
          </div>
          {% endif %}
        {% endif %}
      {% endfor %} 
    {% endif %}#}