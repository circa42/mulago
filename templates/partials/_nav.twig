
{# establishes if the user is on that page #}
{% set segment = craft.app.request.segments|first %}




<div class="font-sans antialiased w-full z-40 bg-white md:opacity-90 shadow-lg fixed h-24" id="mobile">

  <nav class="flex justify-between flex-wrap p-6">
    <div class="flex flex-shrink text-white mr-6 pb-4">
      <div class="h-8 mr-2 flex-shrink xs:w-32 lg:w-56 m-auto">
        <a href="{{ siteUrl }}">
          <img src="{{ siteUrl }}assets/img/mulago-signature600.png" alt="{{ tagline.text }}">
        </a>
      </div>
    </div>
    <div class="block md:hidden">
      <button @click="toggle" class="flex px-3 py-2 border rounded text-blue-700 border-blue-700 hover:text-red hover:border-red">
        <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
      </button>
    </div>
    

    <div :class="open ? 'block': 'hidden'" class="bg-white w-full flex-grow md:flex md:items-center md:w-auto">
        <div class="md:flex md:flex-grow justify-end">
          

{# 
          {% for row in navigation.navTable %}
            <div id="dropdown">
              <div class="text-center pl-2 xs:border-0 my-4">                    
                  
                    {% if (row.linkUrl == true) and (row.subMenu == false) %}                     
                        <div>
                          <a href="{{ siteUrl }}{{ row.linkUrl }}" class="uppercase text-xs no-underline mt-2 md:mt-0 text-gray-900 border-transparent border-t-2 hover:border-red {% if segment == row.linkUrl %} border-red {% endif %} xs:text-center p-2">
                            {{ row.linkName }}
                          </a>
                        </div>

                    {% elseif row.linkUrl == false %}  
                      <div @click="toggle" class="cursor-pointer pb-2">
                          <a class="uppercase text-xs no-underline mt-2 md:mt-0 text-gray-900 border-transparent border-t-2 hover:border-red {% if segment == row.linkUrl %} border-red {% endif %} xs:text-center p-2">
                            {{ row.linkName }}
                          </a>
                      </div>
                    {% elseif row.subMenu == true %}
                      <div :class="open ? 'block': 'hidden'" class="absolute">
                        <a href="/fellows/rainer-fellows" class="text-xs uppercase no-underline block text-gray-darkest hover:text-blue-600 hover:bg-gray-100">
                          <div class="block bg-white border-gray-400 border-b-2 py-2 px-8">
                            {{ row.linkName }}
                          </div>
                        </a>
                      </div>
                    {% endif %}
              </div>
            </div>
          {% endfor %}
          
 #}
          


 

          {% for row in navigation.navTable %}
              <div class="text-center pl-2 xs:border-0 my-4">                    
                  
                    {% if row.linkUrl == 'fellows' %}                     
                       {% include "partials/_dropnav.twig" %} 
                     {% else %}   
                      <div>
                        <a href="{{ siteUrl }}{{ row.linkUrl }}" class="uppercase text-xs no-underline mt-2 md:mt-0 text-gray-900 border-transparent border-t-2 hover:border-red 
                      {% if segment == row.linkUrl %} border-red {% endif %} xs:text-center p-2">
                          {{ row.linkName }}
                        </a>
                      </div>
                  {% endif %}
              </div>
          {% endfor %}
          

 








{# search #}
          <div class="border-r border-gray-400 text-center pl-2 xs:border-0 my-4">
            <a href="{{ siteUrl }}search" class="text-xs no-underline  md:mt-0 text-gray-900 hover:text-red text-center ml-2 my-4 pt-4">
              <i class="fas fa-search"></i>
            </a>
          </div>

      </div>

    </div>
  </nav>
</div>
{# bumper div to prevent content under the menu #}
<div class="pb-24 bg-white"></div>

