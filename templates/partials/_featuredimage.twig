{% set image = matrixBlock.featureImage.all() %}

    <div class="flex flex-col bg-gray-200 z-40">

        <div class="relative m-auto">
              
              
              {% set imageQ = matrixBlock.featureImage.all() %}
              {% for image in imageQ %}
                <img src="{{ image.getUrl('view2100x610') }}" alt="">
              {% endfor %}
            <div class="absolute bottom-0 left-0">
              <h1 class="text-white leading-none opacity-50 text-10xl text-uppercase text-shadow font-normal xs:text-6xl md:text-7xl lg:text-10xl ml-8">
                {{ entry.title }}
              </h1>
            </div>
        

        </div>
    </div>
