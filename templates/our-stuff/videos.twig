{% extends "_layout" %}

    {# Videos #}

{% block content %}

{% set ourStuffQ = craft.entries.section('ourstuff') %}
{% set videos = ourStuffQ.all() %}

{# videos #}
  <div class="bg-gray-200 p-12">

    <div class="">
      <div class="flex flex-wrap m-8">
        
          {% for entry in videos %}
            {% if entry.type == 'videos' %}
              {% include "our-stuff/partials/_video-block.twig" %}
            {% endif %}  
          {% endfor %}
      </div>

    </div> 
  </div> 
{% endblock %}